# Move Add Subject inline + Make Subject Optional

## Steps
- [x] 1. Backend: make subject optional in `POST /tasks` (tasks.js).
- [x] 2. TasksScreen: move "Add subject" input beside "New task" input in the add row.
- [x] 3. TasksScreen: make subject optional in `addTask()` (auto-create typed subject).
- [x] 4. TasksScreen: clean up subject card (remove redundant create row).
- [x] 5. Verify no DB migration needed (schema already allows null subject).
